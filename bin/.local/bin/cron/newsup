#!/bin/sh

ping -q -c 1 1.1.1.1 > /dev/null || exit

/usr/bin/notify-send -t 5000 "Updating RSS feeds..."

echo > /tmp/newsupdate
/usr/bin/newsboat -x reload
rm -f /tmp/newsupdate
/usr/bin/notify-send -t 5000 "RSS feed update complete."

