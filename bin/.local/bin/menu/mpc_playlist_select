#!/bin/sh

format="%album% - %title%"
TITLE=$(mpc playlist -f "$format" | rofi -dmenu -i -p "Track:");
if [ "$TITLE" == "" ]; then exit; fi
POS=$(mpc playlist -f "$format" | grep -n "$TITLE" | awk -F: '{print $1}')
mpc play $POS
