#!/bin/bash
set -e
ws=$(swaymsg -t get_workspaces | jq -r .[].name | dmenu -p "Workspace:")
if [ "$1" = "switch" ]
then
    swaymsg "workspace $ws"
else
    swaymsg "move container to workspace $ws"
fi
