#!/bin/sh

selected_config=$(find "$DOTFILES/" -type f \
                  | sed "s@$DOTFILES/@@" \
                  | rofi -dmenu -i -p "Config File")
[ -z $selected_config ] && exit 0;
/usr/bin/konsole -e "vim $DOTFILES/$selected_config"
